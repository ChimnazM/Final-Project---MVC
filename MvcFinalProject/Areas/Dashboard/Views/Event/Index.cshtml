

@model List<Event>
@{
    ViewData["Title"] = "Index";
}
 <div class="main-panel">
       <div class="content-wrapper">
            <div class="row">
             <div class="col-lg-12 stretch-card">
              <div class="card">
                <div class="card-body">
                  <h4 class="card-title">Table with contextual classes</h4>
                  <p class="card-description">
                    Add class <code>.table-{color}</code>
                  </p>
                  <div class="table-responsive pt-3">
                    <table class="table table-bordered">
                      <thead>
                        <tr>
                         <th>
                            Id
                          </th>
                          <th>
                            Image
                          </th>
                          <th>
                            Title
                          </th>
                          <th>
                            Actions
                          </th>
                     
                        </tr>
                      </thead>
                      <tbody>
                        
                        @foreach (var events in Model)
                       {
                           <tr class="table-success">
                         <td>
                           @events.Id
                          </td>
                           <td>
                               
                            <img src=~/Assets/img/event/@events.ImageMain>
                          </td>
                          <td>
                           @events.Title
                          </td>
                        
                          <td>
                            <a class="btn btn-primary" asp-controller="event" asp-action="Detail" asp-route-id="@events.Id">Detail</a>
                            <a class="btn btn-warning" asp-controller="event" asp-action="Update" asp-route-id="@events.Id">Update</a>
                            <a class="btn btn-danger" asp-controller="event" asp-action="delete" asp-route-id="@events.Id">Delete</a>
                          </td>
                      
                        </tr>
                       }
                        
                       
                      </tbody>
                    </table>
                  </div>
                </div>
              </div> 
              </div>
           </div> 
         </div>
 </div>


